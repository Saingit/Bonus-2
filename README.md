
#Bonus 2 curso Promedio de los estudiantes y quienes tiene el promedio alto. nodeJS

funciones a ejecutar desde el index por consola:
mostrar()= node index mostrar

crear() = node index crear

promedioE() = node index promedioE (Debe ingresar a un estudiante existente en el Listado.json)

mostrarAlto() = node index mostrarAlto (muesta a los estudiantes con promedio superior a 4.5)